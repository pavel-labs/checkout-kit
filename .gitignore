# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Real credentials. The .env.example templates beside them are the committed half.
.env
.env.*
!.env.example
!.env.mock

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# ACS simulator self-signed certs (generate locally, never commit)
apps/bank-sim/certs/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.codex
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TanStack Router plugin cache
.tanstack/

# The docs site: its own install, its build output, and the API reference, which is
# generated from the packages' declarations by `npm run docs:api`.
docs/node_modules
docs/.vitepress/dist
docs/.vitepress/cache
docs/api

